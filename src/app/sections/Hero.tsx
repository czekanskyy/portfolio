import Headings from '../components/hero/Headings';
import ScrollPopup from '../components/ScrollPopup';

const Hero = () => {
  return (
    <main>
      {/* <Headings /> */}
      <ScrollPopup />
    </main>
  );
};

export default Hero;
